export enum TodoItemStatus {
  DONE = 'done',
  NOT_DONE = 'not-done',
}
export enum TodoItemPriority {
  MEDIUM = 'medium',
  LOW = 'low',
  HIGH = 'high',
}
export enum TodoItemReccuringInterval {
  DAILY = 'daily',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
}
